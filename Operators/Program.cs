using System;

namespace Operators {
    class Program {
        static void Main(string[] args)
        {
            int a, b, c, d, sonuc;
            a = 5;
            b = 6;
            c = 10;
            d = 20;


           
            /*sonuc = a * (b / c) + d;

            short miktar = 10;
            miktar += 5;
            miktar -= 5;
            miktar *= 5;
            miktar /= 5;

            miktar++; // önce işlem yapılır sonra artırma yapılır
            miktar--; // önce işlem yapılır sonra azaltılır
            ++miktar; // önce bir artırıyor sonra işlem yapıyors



            // karşılaştırma operatörleri
            int s1 = 90;
            int s2 = 99;
            bool kucukmu = s1 > s2; //false


            // as is operatorleri
        //    object obj = "Aksakal ";
         //   string yazi = obj as string;
          //  bool stringmi = yazi is string;// true
          //  bool intmi = yazi is int; // false
            bool objmi = yazi is object;

            // Mantıksal operatörler

          //  bool notKontrol = s1 <= 10 || s1 >= 65;
          //  bool kont = !(s1 != 60 %% s2 < 40);// true 


            // Bitsel ve, veyaOperatörler
            byte bSayi = 5;
            byte iSayi = 3;
            int kont7 = bSayi & iSayi; // => 0001
            int kont8 = bSayi | iSayi; // => 0111

            // // bitsel özel veya
            // << >> bir bit sağa veya sola kaydırır bu da sayının iyiye bölümü veya 2 ile çarpımı
            // bir sayının tildasını aldığımızda ikilik  ter çeviriyor
            /// 000000101 = 5=> 11111010 =255-5=250
            /// //
            
            // int bSayi = (byte)~iSayi;
      */
            
            
        }
    }
}
